# Barcode Mint Vertical Keyword Pages Task List

Created: 2026-06-08

Source PRD: low-KD vertical page expansion for garment, MRP sticker, LPN, and inventory barcode label use cases.

Implementation scope: implement all core, template, batch, LPN sequence, FAQ schema, reverse-link, advanced label-size, Avery/template, ZIP, local history, field-mapping, and template-editor requirements from the PRD. Do not split the work into P0/P1/P2 phases.

## 1. Current Project Baseline

- [ ] Confirm the working tree before edits and avoid overwriting unrelated user changes.
  - Current note: `docs/task-list.md` is deleted in the working tree at task-list planning time; do not restore or overwrite it unless explicitly requested.
- [ ] Re-read required project docs before implementation:
  - `docs/prd-mvp.md`
  - `docs/acceptance.md`
  - `docs/ui-ux-guidelines.md`
  - `docs/task-list.md` if it exists again when implementation starts.
- [ ] Confirm Nuxt structure:
  - Pages live in `app/pages/`.
  - Shared SEO content lives in `utils/seoPages.ts`.
  - Page head and JSON-LD are generated by `app/composables/useToolPageHead.ts`.
  - Generic single-page wrapper is `app/components/seo/ToolPage.vue`.
  - SEO body renders through `app/components/seo/SeoContent.vue`, `FaqBlock.vue`, and `RelatedTools.vue`.
  - Static sitemap is `public/sitemap.xml`.
- [ ] Confirm current tool components and their limits:
  - `BarcodeGenerator.vue` supports single barcode only with `defaultType` and `defaultValue`.
  - `LabelDesigner.vue` supports simple/product/inventory labels, one repeated barcode value, PNG/SVG/PDF export.
  - `BulkBarcodeGenerator.vue` supports one barcode value per line and PDF/SVG export.
  - `SpreadsheetBarcodeGenerator.vue` supports 3 spreadsheet columns: barcode value, label text, extra text.
  - `parseSpreadsheetPaste.ts` parses tab/comma rows but has no scenario-specific schema.
  - `labelTemplates.ts` only has `simple`, `product`, and `inventory`.
  - `exportPdf.ts` only has single barcode, generic bulk barcode, and repeated single-label sheet PDF exports.

## 2. Data Model And Configuration Design

- [ ] Add a vertical tool configuration model instead of hard-coding four separate copied tools.
  - Suggested file: `utils/verticalBarcodePages.ts` or extend `utils/seoPages.ts` only if the file stays readable.
  - Define `BarcodePageContext`:
    - `home`
    - `printable`
    - `sku`
    - `label`
    - `excel`
    - `bulk`
    - `garment`
    - `mrp`
    - `lpn`
    - `inventory`
  - Define `BarcodeMode`:
    - `single`
    - `bulk`
    - `label`
  - Define vertical template IDs:
    - `garment`
    - `mrp`
    - `lpn`
    - `inventory`
  - Keep existing label template IDs if needed, but map user-facing names clearly:
    - Garment Label
    - MRP Price Sticker
    - LPN Warehouse Label
    - Inventory Label
- [ ] Define reusable vertical sample-row types.
  - `barcodeValue` is always required.
  - Optional shared fields: `productName`, `labelText`, `location`, `quantity`, `category`, `batch`, `packSize`, `salePrice`, `mrp`, `color`, `size`, `style`, `warehouse`, `unitType`.
- [ ] Define page-specific configurations:
  - `/garment-barcode-generator`
    - default mode: `label`
    - default barcode type: `code128`
    - default template: `garment`
    - default value: `TSHIRT-BLK-M`
    - barcode placeholder: `TSHIRT-BLK-M`
    - label text placeholder: `Black T-Shirt / Size M`
  - `/mrp-sticker-with-barcode-generator`
    - default mode: `label`
    - default barcode type: `code128`
    - default template: `mrp`
    - default value: `SKU-001`
    - barcode placeholder: `SKU-001`
    - label text placeholder: `Product Name`
  - `/lpn-barcode-generator`
    - default mode: `label` or `bulk`, but the page must show LPN label-sheet behavior in the first viewport.
    - default barcode type: `code128`
    - default template: `lpn`
    - default value: `LPN-000001`
    - barcode placeholder: `LPN-000001`
  - `/inventory-barcode-label-generator`
    - default mode: `label`
    - default barcode type: `code128`
    - default template: `inventory`
    - default value: `INV-1001`
    - barcode placeholder: `INV-1001`
    - label text placeholder: `Item Name`
- [ ] Add complete sample rows:
  - Garment:
    - `TSHIRT-BLK-M`, `Black T-Shirt`, `Black`, `M`, `TS-2026`
    - `HOODIE-GRY-L`, `Grey Hoodie`, `Grey`, `L`, `HD-1001`
    - `DRESS-RED-S`, `Red Dress`, `Red`, `S`, `DR-551`
  - MRP:
    - `SKU-001`, `Cotton T-Shirt`, `$19.99`, `$14.99`, `1 pc`
    - `SKU-002`, `Coffee Mug`, `$12.99`, empty sale price, `1 pc`
    - `SKU-003`, `Notebook Pack`, `$8.99`, `$6.99`, `Pack of 3`
  - LPN:
    - `LPN-000001`, `WH-A1`, `Pallet`
    - `LPN-000002`, `WH-A2`, `Carton`
    - `LPN-000003`, `WH-B1`, `Bin`
  - Inventory:
    - `INV-1001`, `Shipping Box`, `WH-A1`, `24`
    - `INV-1002`, `Packing Tape`, `WH-B2`, `12`
    - `INV-1003`, `Product Sample`, `Shelf-03`, `8`

## 3. Shared Tool Architecture

- [ ] Decide the component strategy after reading the current components again.
  - Preferred direction: create a scenario-aware wrapper such as `VerticalBarcodeTool.vue` that composes single, bulk, spreadsheet, and label behavior without duplicating the four pages.
  - Keep existing public behavior of `BarcodeGenerator.vue`, `BulkBarcodeGenerator.vue`, `SpreadsheetBarcodeGenerator.vue`, and `LabelDesigner.vue`.
- [ ] Extract duplicated download helpers.
  - Move duplicate `downloadBlob`, `svgToPngBlob`, and `sanitizeFilePart` logic into a utility if it reduces duplication without changing behavior.
  - Verify all existing PNG/SVG/PDF downloads still use valid filenames.
- [ ] Add configurable props to label/bulk/spreadsheet components as needed.
  - `defaultBarcodeType`
  - `defaultValue`
  - `defaultTemplate`
  - `context`
  - `placeholder`
  - `labelTextPlaceholder`
  - `sampleRows`
  - `defaultSpreadsheetInput`
  - `fieldSchema`
  - `enableSequenceGenerator`
- [ ] Keep all barcode data local to the browser.
  - Do not add server APIs.
  - Do not log raw barcode values in analytics.
  - Existing analytics events must continue to send only safe metadata such as type, row count, value length, template, and error counts.
- [ ] Preserve the existing tool tabs and make them scenario-aware.
  - Single Barcode
  - Bulk Barcodes
  - Excel Paste
  - Label Sheet
  - Avoid tabs linking users away when the current vertical page can support the mode inline.
- [ ] Ensure all inputs have labels.
  - Barcode value
  - Product name
  - Color
  - Size
  - Style number
  - MRP / Price
  - Sale price
  - Pack / Size
  - Batch
  - Warehouse / Location
  - Carton / Pallet / Bin
  - Item name
  - Quantity
  - Category
  - Prefix
  - Start number
  - Sequence quantity
  - Digits

## 4. Label Template Engine

- [ ] Extend `utils/labelTemplates.ts` to support scenario templates.
  - Add template definitions for Garment Label, MRP Price Sticker, LPN Warehouse Label, and Inventory Label.
  - Keep current `simple`, `product`, and `inventory` compatibility for existing pages.
- [ ] Replace rigid `getLabelTextLines` logic with a template-aware line builder.
  - Garment layout:
    - Product Name
    - `Style: TS-2026 / Color: Black / Size: M`
    - barcode image
    - barcode value
  - MRP layout:
    - Product Name
    - `MRP / Price: $19.99`
    - optional `Sale: $14.99`
    - optional `Pack: 1 pc`
    - barcode image
    - barcode value
  - LPN layout:
    - LPN value as a large top line
    - optional `Warehouse: A1`
    - barcode image
    - `License Plate Number`
  - Inventory layout:
    - Item Name
    - optional `Location: WH-A1 / Qty: 24`
    - barcode image
    - barcode value
- [ ] Update visual preview rendering in `LabelDesigner.vue` or the new vertical tool.
  - Each vertical template must look structurally different, not just swap text.
  - Preview must not overflow horizontally on mobile.
  - Barcode preview must remain readable in 2 x 1, 3 x 2, and added label sizes.
- [ ] Update PDF label rendering in `utils/exportPdf.ts`.
  - PDF output must use the same scenario fields as the screen preview.
  - Support multiple unique rows in a label sheet, not only one repeated label.
  - Keep the print hint: `For best results, print at 100% scale and disable "Fit to page".`
- [ ] Add PDF rendering support for optional fields.
  - Empty optional fields must not leave awkward blank gaps.
  - Long fields must be trimmed or wrapped safely.
  - PDF text must be escaped correctly.

## 5. Batch And Spreadsheet Field Parsing

- [ ] Extend spreadsheet parsing beyond the generic 3-column model.
  - Keep `parseSpreadsheetPaste.ts` generic and add a higher-level field mapper for scenario pages.
  - Support tab-delimited rows from Excel and Google Sheets.
  - Support simple CSV rows with quoted cells.
- [ ] Add scenario-specific column schemas.
  - Garment:
    - Column 1: Barcode Value
    - Column 2: Product Name
    - Column 3: Color
    - Column 4: Size
    - Column 5: Style
    - Column 6: Optional Price
  - MRP:
    - Column 1: Barcode Value
    - Column 2: Product Name
    - Column 3: MRP / Price
    - Column 4: Sale Price optional
    - Column 5: Pack / Size optional
    - Column 6: Batch optional
  - LPN:
    - Column 1: LPN Value
    - Column 2: Warehouse / Location optional
    - Column 3: Carton / Pallet / Bin optional
    - Column 4: Batch optional
  - Inventory:
    - Column 1: Barcode Value
    - Column 2: Item Name
    - Column 3: Location optional
    - Column 4: Quantity optional
    - Column 5: Category optional
- [ ] Display parse stats near the paste input:
  - Rows detected
  - Valid rows
  - Rows with errors
- [ ] Add clear row-level errors:
  - Missing barcode value
  - Missing product name
  - Invalid price format
  - Invalid quantity
  - Invalid sequence quantity
  - Barcode validation error from `validateBarcode`
- [ ] Keep bulk limits explicit.
  - Existing limit is 100 rows.
  - If ZIP export or Avery layouts require a higher or lower practical limit, show the limit in the UI and error message.
- [ ] Add CSV/template downloads for each scenario.
  - Garment CSV template.
  - MRP CSV template.
  - LPN CSV template.
  - Inventory CSV template.
  - Advanced field-mapping templates if the template editor is enabled.

## 6. LPN Sequence Generator

- [ ] Add a lightweight LPN sequence generator utility.
  - Suggested file: `utils/lpnSequence.ts`.
  - Inputs:
    - Prefix
    - Start Number
    - Quantity
    - Digits
  - Output:
    - `LPN-000001`
    - `LPN-000002`
    - `LPN-000003`
- [ ] Validate sequence fields.
  - Prefix cannot be empty after trimming.
  - Start number must be a positive integer or zero if explicitly supported.
  - Quantity must be between 1 and the current batch limit.
  - Digits must be large enough for the largest generated number.
- [ ] Add LPN sequence UI to `/lpn-barcode-generator`.
  - Show the fields in the first tool area.
  - Provide a `Generate LPN sequence` action.
  - Populate the batch rows and label sheet preview from the generated sequence.
- [ ] Add tests or targeted checks for sequence generation.
  - `LPN-`, start 1, quantity 10, digits 6 produces `LPN-000001` through `LPN-000010`.
  - Invalid quantity produces a clear error.
  - Digits smaller than the final number length produces a clear error or expands safely by design.

## 7. Advanced Label Sizes, Paper Templates, And Avery Templates

- [ ] Expand `LABEL_SIZES` beyond current `2x1` and `3x2`.
  - Add small, medium, shipping/carton, and warehouse-friendly options.
  - Keep mobile controls readable and avoid oversized select labels.
- [ ] Add more paper templates.
  - US Letter.
  - A4.
  - Common sheet margins and gaps.
  - Label pitch settings when needed.
- [ ] Add Avery-style templates without claiming official certification.
  - Use neutral wording such as `Avery-compatible layout`.
  - Include fields for page size, label width, label height, top margin, left margin, horizontal gap, vertical gap, rows, and columns.
  - Add at least a small starter set of common layouts if exact template data is verified before implementation.
- [ ] Add custom layout controls.
  - Label width.
  - Label height.
  - Page size.
  - Margins.
  - Gap.
  - Rows and columns.
- [ ] Update PDF generation to use layout definitions rather than hard-coded margins.
  - Existing hard-coded values in `buildLabelSheetPages` must become configurable.
  - Existing behavior must remain the default for old label pages.
- [ ] Add print safety checks.
  - Warn if label dimensions do not fit the selected paper.
  - Warn if rows/columns exceed printable area.
  - Keep print scale advice visible.

## 8. ZIP Export

- [ ] Add batch ZIP download for generated image files.
  - Scope: SVG ZIP first if no dependency is needed.
  - PNG ZIP requires async canvas conversion for multiple rows; implement only after evaluating performance.
- [ ] Avoid adding a heavy dependency unless clearly justified.
  - If using a ZIP dependency, document why browser-native alternatives are insufficient.
  - If no dependency is used, implement a focused ZIP writer utility or use a browser-compatible lightweight approach.
- [ ] Add ZIP export controls only when there is at least one valid row.
  - Disabled state for invalid/empty batches.
  - Clear error if ZIP creation fails.
- [ ] Name ZIP files by context.
  - `garment-barcode-labels.zip`
  - `mrp-barcode-stickers.zip`
  - `lpn-barcode-labels.zip`
  - `inventory-barcode-labels.zip`
- [ ] Track ZIP export analytics without raw barcode values.

## 9. Local History

- [ ] Add browser-local recent batches/history.
  - Use `localStorage`.
  - Store only user-owned local data and make the behavior explicit in copy.
  - Provide a clear history delete action.
- [ ] Keep history optional and lightweight.
  - Do not add login.
  - Do not add database.
  - Do not upload barcode values.
- [ ] Define history entries.
  - Context.
  - Template.
  - Barcode type.
  - Timestamp.
  - Row count.
  - Rows or field values if local persistence is intentionally enabled.
- [ ] Add UI for restoring a recent batch.
  - Keep it below the primary input area so it does not push the tool below the first viewport.
  - On mobile, use a compact collapsible section.
- [ ] Add clear privacy copy:
  - `Recent batches are saved only in this browser. You can clear them at any time.`

## 10. Advanced Field Mapping And Template Editor

- [ ] Add field mapping for pasted spreadsheet columns.
  - Let users map columns to scenario fields.
  - Provide presets for garment, MRP, LPN, and inventory.
  - Keep the default path simple: pasted rows should work immediately with the documented order.
- [ ] Add a template editor model.
  - Template name.
  - Field order.
  - Show/hide fields.
  - Static text lines.
  - Barcode position.
  - Font size options within safe bounds.
  - Label size/layout association.
- [ ] Add a constrained visual template editor.
  - Do not make a heavy design tool.
  - Use existing form controls: selects, toggles, checkboxes, numeric inputs.
  - Keep preview synchronized.
- [ ] Save custom templates locally.
  - Use `localStorage`.
  - Provide reset to default templates.
  - Do not sync templates to a server.
- [ ] Ensure custom templates work with PDF, SVG, PNG, and ZIP exports.
  - If custom template export has limitations, show clear copy in the UI and document it in code comments.

## 11. New Page Implementation

- [ ] Add `app/pages/garment-barcode-generator.vue`.
  - Use vertical tool config.
  - Use `useToolPageHead`.
  - H1: `Free Garment Barcode Generator for Clothing Labels`
  - Subtitle: `Generate barcodes for clothing SKUs, sizes, colors, and apparel inventory labels. Create printable label sheets for garments, products, and stock rooms.`
  - Tool first viewport:
    - Label Sheet mode.
    - Code 128.
    - Garment Label template.
    - Placeholder `TSHIRT-BLK-M`.
    - Label text placeholder `Black T-Shirt / Size M`.
  - Include garment-specific content sections:
    - How to create garment barcode labels.
    - Best barcode type for clothing SKUs.
    - Clothing barcode label use cases.
    - Bulk clothing barcode generation.
  - Include garment-specific FAQ:
    - What is a garment barcode?
    - What barcode type should I use for clothing SKUs?
    - Can I create barcode labels for clothing sizes and colors?
    - Can I paste clothing SKUs from Excel?
    - Can I print garment barcode labels as PDF?
    - Do I need a UPC barcode for clothing products?
- [ ] Add `app/pages/mrp-sticker-with-barcode-generator.vue`.
  - H1: `Free MRP Sticker with Barcode Generator`
  - Title: `Free MRP Sticker with Barcode Generator - Create Printable Price Labels`
  - Description: `Create printable MRP stickers with barcodes for products, SKUs, and price labels. Add product name, price, and barcode, then export as PDF.`
  - Tool first viewport:
    - Label Sheet mode.
    - Code 128.
    - MRP Price Sticker template.
    - Placeholder `SKU-001`.
    - Label text placeholder `Product Name`.
  - Include compliance note:
    - `This tool helps create printable barcode price stickers. Please make sure your product labels meet the pricing and labeling rules that apply in your market.`
  - Include MRP-specific sections:
    - What is an MRP sticker with barcode?
    - How to create an MRP barcode sticker.
    - Barcode type for price stickers.
    - Printing price stickers.
  - Include MRP-specific FAQ:
    - Can I create an MRP sticker with a barcode online?
    - Can I add product name and price to the sticker?
    - Can I export MRP barcode stickers as PDF?
    - What barcode type should I use for price stickers?
    - Can I paste product and price data from Excel?
    - Are these labels compliant for retail use?
- [ ] Add `app/pages/lpn-barcode-generator.vue`.
  - H1: `Free LPN Barcode Generator`
  - Title: `Free LPN Barcode Generator for Warehouse and Inventory Labels`
  - Description: `Generate LPN barcodes for warehouse labels, pallets, cartons, and inventory tracking. Create Code 128 LPN labels and export printable PDF sheets.`
  - Tool first viewport:
    - Label Sheet or Bulk mode with LPN Warehouse Label visible.
    - Code 128.
    - Placeholder `LPN-000001`.
    - Sequence generator visible.
  - Include LPN-specific sections:
    - What is an LPN barcode?
    - Best barcode type for LPN labels.
    - LPN barcode examples.
    - Warehouse label printing tips.
  - Include LPN-specific FAQ:
    - What does LPN mean in warehouse labels?
    - What barcode type should I use for LPN barcodes?
    - Can I generate multiple LPN barcodes at once?
    - Can I create pallet and carton barcode labels?
    - Can I export LPN labels as PDF?
    - Can I paste LPN values from Excel?
- [ ] Add `app/pages/inventory-barcode-label-generator.vue`.
  - H1: `Free Inventory Barcode Label Generator`
  - Title: `Free Inventory Barcode Label Generator for Products and Stock`
  - Description: `Create printable inventory barcode labels for products, SKUs, stock rooms, and warehouse items. Generate labels in bulk and export as PDF.`
  - Tool first viewport:
    - Label Sheet mode.
    - Code 128.
    - Inventory Label template.
    - Placeholder `INV-1001`.
    - Label text placeholder `Item Name`.
  - Include inventory-specific sections:
    - How to create inventory barcode labels.
    - Inventory barcode examples.
    - Inventory vs SKU vs LPN barcode comparison table.
    - Bulk inventory barcode labels.
  - Include inventory-specific FAQ:
    - What is an inventory barcode label?
    - What barcode type should I use for inventory labels?
    - Can I create inventory barcode labels in bulk?
    - Can I add item names and locations to labels?
    - Can I export inventory labels as PDF?
    - What is the difference between SKU, inventory, and LPN barcodes?

## 12. SEO Content And Schema

- [ ] Add new page entries to the central SEO content source.
  - Unique `title`.
  - Unique `description`.
  - Unique `h1`.
  - Unique `subtitle`.
  - Unique FAQ.
  - Unique sections.
  - Related tools.
  - Featured fields/tool context if the shared content type is expanded.
- [ ] Confirm canonical URLs are correct through `useToolPageHead`.
  - `https://www.barcode-mint.com/garment-barcode-generator`
  - `https://www.barcode-mint.com/mrp-sticker-with-barcode-generator`
  - `https://www.barcode-mint.com/lpn-barcode-generator`
  - `https://www.barcode-mint.com/inventory-barcode-label-generator`
- [ ] Confirm FAQPage schema includes each page's unique FAQ.
- [ ] Confirm SoftwareApplication schema has the correct page URL and page name.
- [ ] Confirm BreadcrumbList schema points to the current page.
- [ ] Do not add `noindex`.
- [ ] Do not block new URLs in `robots.txt`.
- [ ] Avoid claims that:
  - The tool issues official GS1 barcodes.
  - Generated UPC/EAN values are official product registrations.
  - MRP stickers are legally compliant.
  - The site can verify product authenticity.

## 13. Homepage Internal Link Module

- [ ] Update the existing homepage featured-tools module in `ToolPage.vue` or page content.
  - Required title: `Choose the right barcode generator`.
  - Required subtitle: `Create printable barcode labels for products, clothing, inventory, warehouse operations, and price stickers.`
- [ ] Add homepage cards with title links:
  - `Garment Barcode Generator` -> `/garment-barcode-generator`
    - `Create clothing barcode labels for SKUs, sizes, colors, and apparel inventory.`
  - `MRP Sticker with Barcode Generator` -> `/mrp-sticker-with-barcode-generator`
    - `Create printable price stickers with product names, MRP, and barcodes.`
  - `LPN Barcode Generator` -> `/lpn-barcode-generator`
    - `Generate warehouse LPN barcodes for pallets, cartons, bins, and inventory units.`
  - `Inventory Barcode Label Generator` -> `/inventory-barcode-label-generator`
    - `Create inventory barcode labels for products, stock rooms, and warehouse items.`
  - `SKU Barcode Generator` -> `/sku-barcode-generator`
    - `Create Code 128 barcodes for custom SKU and product codes.`
  - `Barcode Generator for Excel` -> `/barcode-generator-for-excel`
    - `Paste rows from Excel or Google Sheets and generate barcodes in bulk.`
- [ ] Place this module below the homepage tool area and above SEO content.
- [ ] Ensure mobile layout is single column and desktop layout is 2 to 3 columns.
- [ ] Ensure card titles themselves are links and do not rely on `Learn more`.

## 14. Related Tools And Reverse Internal Links

- [ ] Add Related Tools to each new page.
  - Garment:
    - SKU Barcode Generator
    - Barcode Label Generator
    - Barcode Generator for Excel
    - Printable Barcode Generator
    - Bulk Barcode Generator
  - MRP:
    - Barcode Label Generator
    - Printable Barcode Generator
    - SKU Barcode Generator
    - Barcode Generator for Excel
    - Bulk Barcode Generator
  - LPN:
    - Inventory Barcode Label Generator
    - Bulk Barcode Generator
    - Barcode Generator for Excel
    - Barcode Label Generator
    - SKU Barcode Generator
  - Inventory:
    - LPN Barcode Generator
    - SKU Barcode Generator
    - Barcode Generator for Excel
    - Bulk Barcode Generator
    - Barcode Label Generator
- [ ] Add reverse links from existing pages.
  - `/barcode-label-generator` links to:
    - `/garment-barcode-generator`
    - `/mrp-sticker-with-barcode-generator`
    - `/inventory-barcode-label-generator`
  - `/barcode-generator-for-excel` links to:
    - `/garment-barcode-generator`
    - `/inventory-barcode-label-generator`
    - `/lpn-barcode-generator`
  - `/bulk-barcode-generator` links to:
    - `/lpn-barcode-generator`
    - `/inventory-barcode-label-generator`
- [ ] Consider adding natural contextual links from:
  - `/sku-barcode-generator` to garment, inventory, and LPN pages.
  - `/printable-barcode-generator` to MRP, inventory, and garment pages.
  - `/code-128-barcode-generator` to SKU, garment, inventory, and LPN pages.
- [ ] Verify no new page is an orphan page.

## 15. Sitemap And Static Output

- [ ] Update `public/sitemap.xml`.
  - Include:
    - `https://www.barcode-mint.com/garment-barcode-generator`
    - `https://www.barcode-mint.com/mrp-sticker-with-barcode-generator`
    - `https://www.barcode-mint.com/lpn-barcode-generator`
    - `https://www.barcode-mint.com/inventory-barcode-label-generator`
  - Confirm existing key URLs remain:
    - `/`
    - `/sku-barcode-generator`
    - `/printable-barcode-generator`
    - `/barcode-label-generator`
    - `/barcode-generator-for-excel`
    - `/bulk-barcode-generator`
    - `/code-128-barcode-generator`
    - `/upc-barcode-generator`
    - `/upc-a-barcode-generator`
    - `/ean-13-barcode-generator`
- [ ] Add `lastmod` only if the sitemap format is updated consistently.
  - If `lastmod` is added, apply it consistently enough to avoid an uneven sitemap.
- [ ] After `pnpm generate`, check generated routes in `dist/`.
- [ ] Locally verify sitemap output contains all four new full URLs.

## 16. Styling, UX, And Accessibility

- [ ] Keep the tool visible in the first viewport on all four pages.
- [ ] Keep SEO content below the tool.
- [ ] Ensure all interactive controls have minimum 44px tap height on mobile.
- [ ] Ensure all form inputs have visible labels.
- [ ] Ensure error messages are close to the relevant input.
- [ ] Ensure download buttons are visible and disabled when invalid.
- [ ] Ensure barcode previews do not overflow horizontally.
- [ ] Ensure bulk tables become readable on mobile.
  - Existing tables may need card-style rows for vertical pages.
- [ ] Avoid large illustration-first or marketing-style hero layouts.
- [ ] Use the existing clean business-friendly visual language.
- [ ] Do not add ads, popups, login, pricing, account flows, server databases, or APIs.
- [ ] Add focused empty, valid, invalid, and over-limit states for the new batch tools.
- [ ] Add a privacy note to vertical tools:
  - User-entered barcode values are processed in the browser.

## 17. Tests And Targeted Verification

- [ ] Run static and type checks available in this project:
  - `pnpm typecheck`
  - `pnpm generate`
  - `pnpm build` if needed after `pnpm generate`
  - No lint script exists in `package.json` at planning time.
- [ ] Add or run targeted unit checks where practical for pure utilities:
  - label line builder
  - scenario spreadsheet mapper
  - LPN sequence generator
  - PDF layout calculation
  - ZIP utility if added
- [ ] Manual browser checks for each new page:
  - Page returns 200 locally.
  - Exactly one H1.
  - Correct title.
  - Correct meta description.
  - Correct canonical.
  - Tool appears in first viewport.
  - Default mode and template match the PRD.
  - Code 128 is selected.
  - Valid default preview appears.
  - Invalid input shows clear error.
  - PNG download works.
  - SVG download works.
  - PDF export works.
  - Batch paste works.
  - Field errors are visible.
  - Related Tools links work.
- [ ] Manual browser checks for existing pages:
  - Homepage tool still works.
  - `/barcode-label-generator` still exports PDF and PNG/SVG.
  - `/barcode-generator-for-excel` still parses current 3-column examples.
  - `/bulk-barcode-generator` still parses one-value-per-line input.
  - `/sku-barcode-generator` still recommends Code 128 behavior through validation copy.
- [ ] Verify no raw barcode values are emitted in analytics payloads.

## 18. Visual QA Requirement

- [ ] Because this implementation changes UI, layout, responsive behavior, navigation, SEO inner-link modules, and tool first-screen display, use the required visual QA skill before completion:
  - `$visual-qa-screenshots`
- [ ] Generate static output first:
  - `pnpm generate`
- [ ] Capture screenshots with:
  - `node ~/.codex/skills/visual-qa-screenshots/scripts/capture_web_screenshots.mjs`
- [ ] Cover at least desktop and mobile viewports for:
  - `/`
  - `/garment-barcode-generator`
  - `/mrp-sticker-with-barcode-generator`
  - `/lpn-barcode-generator`
  - `/inventory-barcode-label-generator`
  - Any old pages whose reverse-link or related-tools sections changed.
- [ ] Include machine assertions:
  - key page copy exists.
  - H1 count is 1.
  - no horizontal overflow.
  - key selectors exist for tool, FAQ, Related Tools, and homepage internal-link module.
- [ ] Use `zai-mcp-server` UI/UX audit in parallel for multiple screenshots.
  - Desktop and mobile screenshots should be analyzed in one parallel batch when several images need review.
- [ ] Fix any visual issues found before final verification.

## 19. Deployment And Production Verification

- [ ] Before deploy, confirm all checks pass locally.
- [ ] Commit the implementation with a clear commit message.
  - Include context, approach, tested commands, and not-tested gaps if any.
- [ ] Push to the current branch's remote unless explicitly told not to push.
- [ ] Deploy to Cloudflare Pages.
  - Run `pnpm generate` immediately before deploy.
  - Use `dist` as deploy directory.
  - If the Pages project name is not clearly recorded, run `pnpm wrangler pages project list`.
  - Deploy with `pnpm wrangler pages deploy dist --project-name <pages-project-name>`.
- [ ] Verify production at `https://www.barcode-mint.com`.
  - Use short boolean checks, not full HTML dumps.
  - Confirm all four new URLs return production content.
  - Confirm sitemap includes the four new URLs.
  - Confirm homepage contains the new internal-link module.
- [ ] If IndexNow submission is still part of the current workflow, run the existing script after deploy:
  - `pnpm indexnow`
  - Do not submit URLs before production content is verified.

## 20. Final Acceptance Checklist

- [ ] New pages exist and are indexable:
  - `/garment-barcode-generator`
  - `/mrp-sticker-with-barcode-generator`
  - `/lpn-barcode-generator`
  - `/inventory-barcode-label-generator`
- [ ] Each new page has:
  - unique title.
  - unique meta description.
  - unique H1.
  - canonical.
  - FAQ block.
  - FAQPage schema.
  - SoftwareApplication schema.
  - BreadcrumbList schema.
  - Related Tools.
  - scene-specific sample data.
  - scene-specific label template.
  - tool in the first viewport.
- [ ] Default states match:
  - Garment: Label Sheet, Garment Label, Code 128.
  - MRP: Label Sheet, MRP Price Sticker, Code 128.
  - LPN: Bulk or Label Sheet, LPN Warehouse Label, Code 128.
  - Inventory: Label Sheet, Inventory Label, Code 128.
- [ ] Tool functions pass:
  - single barcode input.
  - live preview.
  - validation state.
  - PNG download.
  - SVG download.
  - PDF export.
  - batch paste.
  - scenario field parsing.
  - row-level errors.
  - LPN sequence generation.
  - label sheet with multiple unique rows.
  - ZIP export.
  - local history restore and clear.
  - advanced field mapping.
  - custom template editor.
- [ ] Homepage contains:
  - `Choose the right barcode generator`
  - links to Garment, MRP, LPN, Inventory, SKU, and Excel pages.
- [ ] Existing pages contain required reverse links.
- [ ] Sitemap contains all required URLs.
- [ ] Visual QA desktop and mobile screenshots pass.
- [ ] `pnpm typecheck` passes.
- [ ] `pnpm generate` passes.
- [ ] Deployment succeeds.
- [ ] Production verification passes.

